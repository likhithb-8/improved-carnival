# 🛡️ Improved Carnival (Security Enhanced)

## Security Features
- **Linting:** Uses `eslint-plugin-security` to detect vulnerabilities.
- **Auditing:** Integrated `pnpm audit` for dependency checks.

## Usage
```bash
pnpm install
pnpm lint   # Security check
pnpm audit  # Dependency audit
```
